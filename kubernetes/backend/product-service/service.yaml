apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: cloudcart

spec:
  selector:
    app: product-service

  ports:
  - name: http
    protocol: TCP
    port: 8000
    targetPort: 8000

  type: ClusterIP

