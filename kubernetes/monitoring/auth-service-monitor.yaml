apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: auth-service-monitor
  namespace: monitoring

spec:
  selector:
    matchLabels:
      app: auth-service

  namespaceSelector:
    matchNames:
      - cloudcart

  endpoints:
  - port: http
    path: /metrics
    interval: 15s
